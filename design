
Regexp::Object
==============

exports:

	terminal(object)
	union(items)
	seq(items)
	star(items)
	plus(items)
	opt(items)
	count(min, max, items)
	NO_BOUND
	capture(name, items)
	capture_all(name, items)

The interface for terminal objects

	result = match(input)
		returns undef if not a match

